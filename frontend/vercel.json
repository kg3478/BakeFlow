{
  "version": 2,
  "cleanUrls": true,
  "comment": "BakeFlow is primarily deployed as a single Render service (backend + frontend together). This vercel.json is provided only if you want to deploy the frontend separately to Vercel while pointing API calls to your Render backend. Set VITE_API_URL or update the routes below with your actual Render URL.",
  "routes": [
    {
      "src": "/css/(.*)",
      "dest": "/css/$1"
    },
    {
      "src": "/js/(.*)",
      "dest": "/js/$1"
    },
    {
      "src": "/api/(.*)",
      "dest": "https://YOUR_RENDER_APP.onrender.com/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
